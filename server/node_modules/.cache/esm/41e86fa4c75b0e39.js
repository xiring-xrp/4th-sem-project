let mongoose;_c75‍.x([["default",()=>_c75‍.o]]);_c75‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_c75‍.w("dotenv/config");

const connectToDB = async () => {
  try {
    const { connection } = await mongoose.connect(process.env.MONGODB_URL);
    if (connection) {
      console.log(`connected to DB:${connection.host}`);
    }
  } catch (error) {
    _c75‍.g.console.log(error);
    process.exit(1);
  }
};
_c75‍.d(connectToDB);